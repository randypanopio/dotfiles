# autocomplete?
autoload -Uz compinit
compinit
{{- if eq .chezmoi.os "darwin" }} 
# macOS exported paths, TODO audit these
export PATH="/opt/homebrew/bin:$PATH" 
# export PATH="/Library/Frameworks/Python.framework/Versions/3.11/bin:$PATH"
export PATH="/usr/local/bin:$PATH"
export PATH="/usr/bin:$PATH"
export PATH="/bin:$PATH"
export PATH="/usr/sbin:$PATH"
export PATH="/sbin:$PATH"

export PATH="/System/Cryptexes/App/usr/bin:$PATH"

export PATH="/usr/local/share/dotnet:$PATH"
export PATH="~/.dotnet/tools:$PATH"
export PATH="/opt/homebrew/bin:$PATH"
export PATH="/opt/homebrew/bin$PATH"

{{- if or (eq .chezmoi.arch "amd64") (eq .chezmoi.arch "x86_64") }}
# intel/amd 64 exports
{{ end -}}
{{- if eq .chezmoi.arch "arm64" }} 
# arm64 specific exports
{{ end -}}
{{ end -}}

# zsh exports
export EDITOR=code
export ZSH="$HOME/.oh-my-zsh"

# themes
ZSH_THEME="agnoster"

# Plugins
plugins=(git zsh-autosuggestions) 
source $ZSH/oh-my-zsh.sh

# Aliases
alias py='python3'

{{- if eq .chezmoi.os "linux" }}
{{- if eq .chezmoi.os.name "Ubuntu" }}
{{- if or (eq .chezmoi.arch "amd64") (eq .chezmoi.arch "x86_64") }}

# intel/amd 64 exports
# export brew on Ubuntu (Linux WSL in this case), maybe TODO filter by distro but too lazy
eval "$(/home/linuxbrew/.linuxbrew/bin/brew shellenv)"

{{- else if eq .chezmoi.arch "arm64" }}

# arm exports

{{ end -}}
{{ end -}}
{{ end -}}