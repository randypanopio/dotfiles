[user]
  email = randypanopio@gmail.com
  name = Randy Panopio

[alias]
  squash-all = "!f(){ git reset $(git commit-tree HEAD^{tree} \"$@\");};f"
  chs = !git checkout $1 && git status
  ac = '!f() { git add -A && git commit -m "$@" && return 0; }; f'


[core]
  excludesfile = ~/.gitignore


